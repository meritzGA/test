# 📋 매니저 활동관리 시스템

보험 GA 채널 매니저의 사용인 관리 및 카카오톡 메시지 발송 추적 시스템

## 기능

### 📱 매니저 페이지
- 매니저코드 + 비밀번호 로그인
- 매칭된 사용인 목록 조회
- 4가지 메시지 옵션으로 카카오톡 공유
  - (1) 인사말 보내기
  - (2) 리플렛 보내기
  - (3) 시상 안내하기
  - (4) 시상+실적 안내하기
- 발송 이력 확인 (매월 1일 자동 초기화)

### ⚙️ 관리자 페이지
- 데이터 파일 업로드/삭제/병합 (조인 키 컬럼 선택)
- 시상 JSON 업로드
- 매니저 로그인 현황 모니터링
- 메시지 발송 현황 (유형별/매니저별 인원·횟수)

## 설치 및 실행

```bash
pip install -r requirements.txt
streamlit run app.py
```

## 환경 설정

### Streamlit Cloud
Settings > Secrets에 아래 내용 입력:
```toml
MANAGER_PASSWORD = "meritz1!"
ADMIN_PASSWORD = "admin1!"
KAKAO_JS_KEY = ""
```

### 로컬 실행
`.streamlit/secrets.toml` 파일 생성 (secrets.toml.example 참고)

## 외부 DB 연동

현재 SQLite를 사용합니다. PostgreSQL 등 외부 DB로 전환하려면:

1. `utils/database.py`의 `get_connection()` 함수 수정
2. SQLite 전용 구문(`PRAGMA`, `strftime` 등)을 해당 DB 구문으로 변경
3. `requirements.txt`에 DB 드라이버 추가 (예: `psycopg2-binary`)

## 프로젝트 구조

```
├── app.py                     # 메인 (홈)
├── pages/
│   ├── 1_📱_매니저.py         # 매니저 페이지
│   └── 2_⚙️_관리자.py         # 관리자 페이지
├── utils/
│   ├── database.py            # DB 조작 (SQLite → 외부 DB 전환 가능)
│   └── kakao_share.py         # 카카오톡 공유 컴포넌트
├── requirements.txt
├── .streamlit/
│   ├── config.toml
│   └── secrets.toml.example
└── .gitignore
```
